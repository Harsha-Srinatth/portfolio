# Routes

This directory uses TanStack Router file-based routing.

- `__root.tsx` — root layout for all routes
- `index.tsx` — home page (`/`)

Add new routes by creating files in this folder. The route tree is generated automatically in `src/routeTree.gen.ts`.
